{
  "name": "serverless-handbook",
  "version": "1.0.0",
  "description": "serverless handbook tutorial",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild handler.ts --bundle --outfile=dist/handler.js --platform=node --target=node12",
    "build:prod": "esbuild handler.ts --bundle --outfile=dist/handler.js --platform=node --target=node12 --minify",
    "deploy": "npm run build:prod && serverless deploy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Tagir-A/serverless-handbook.git"
  },
  "author": "tagir-a",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/Tagir-A/serverless-handbook/issues"
  },
  "homepage": "https://github.com/Tagir-A/serverless-handbook#readme",
  "dependencies": {
    "@types/aws-lambda": "^8.10.73",
    "aws-lambda": "^1.0.6"
  },
  "devDependencies": {
    "esbuild": "^0.11.5",
    "serverless": "^2.33.1"
  }
}
